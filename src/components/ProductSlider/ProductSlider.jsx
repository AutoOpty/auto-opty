import styles from './ProductSlider.module.scss';
import React, { useState } from 'react';

const ProductSlider = ({ dataId }) => {
  return <article className={styles.swiperContainer}>Slider</article>;
};

export default ProductSlider;
